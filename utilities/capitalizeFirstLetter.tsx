export const newcityname = function capitalizeFirstLetter(str: string): string {
  //   console.log(str.charAt(0).toUpperCase() + str.slice(1).toLowerCase());
  return str.charAt(0).toUpperCase() + str.slice(1).toLowerCase();
};
